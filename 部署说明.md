# 系统部署说明

## 1. 文档目标

目标是让操作者在一台新的电脑上完成以下事情：

1. 拉取 `yujia-test` 项目源码。
2. 准备运行环境。
3. 启动 openGauss 数据库。
4. 初始化数据库结构与增量脚本。
5. 启动后端 `backend`。
6. 启动后台管理端 `admin-web`。
7. 在微信开发者工具中打开并运行小程序 `uniapp/miniprogram`。
8. 创建可登录后台的管理员账号或业务员账号。
9. 理解常见问题的排查方式。

只要严格按本文档操作，理论上可以独立把整个系统跑起来。

---

## 2. 项目组成

本仓库由三部分组成：

1. `backend`
   Spring Boot 后端服务，端口默认 `8080`。

2. `admin-web`
   Vue 3 后台管理端。

3. `uniapp/miniprogram`
   微信小程序端。

---

## 3. 运行环境要求

### 3.1 操作系统

建议环境：

1. Windows 11
2. 本文档命令以 Windows PowerShell 为主

### 3.2 软件版本要求

请提前安装以下软件：

1. `Git`
   用于拉取代码。

2. `Docker Desktop`
   用于运行 openGauss 3.0 容器。

3. `JDK 17`
   后端 `backend` 使用 Java 17。

4. `Node.js 18+`
   推荐 Node.js 18 或 20。

5. `npm`
   一般随 Node.js 一起安装。

6. `微信开发者工具`
   用于运行小程序端。直接在浏览器搜索安装。

### 3.3 项目内实际版本依据

本项目代码中涉及的主要版本如下：

1. 后端框架：`Spring Boot 3.5.13`
2. Java 版本：`17`
3. MyBatis Spring Boot Starter：`3.0.4`
4. PostgreSQL JDBC 驱动：`42.5.1`
5. 管理端 Vue：`3.5.13`
6. 管理端 Vite：`8.0.4`
7. 管理端 Element Plus：`2.11.1`
8. 管理端 Axios：`1.9.0`
9. 小程序 typings：`miniprogram-api-typings 2.8.3-1`

---

## 4. 拉取源码

如果对方已经有仓库地址，可直接执行：

```powershell
git clone <你的仓库地址>
cd yujia-test
```

拉取完成后，项目根目录应至少看到：

```text
admin-web
backend
docs
uniapp
README.md
```

---

## 5. openGauss 数据库部署

## 5.1 说明

你当前使用的是 Docker 拉取的 `openGauss 3.0`。  
项目后端虽然用的是 PostgreSQL JDBC 驱动，但 openGauss 兼容 PostgreSQL 协议，因此可以直接连接。

当前后端默认数据库配置位于：

[backend/src/main/resources/application.yml](/f:/GraduationProject-yujia/yujia-test/backend/src/main/resources/application.yml)

默认值为：

1. 数据库地址：`localhost`
2. 端口：`15432`
3. 数据库名：`yujia_db`
4. 用户名：`yujia`
5. 密码：`YuJia@123456`

因此建议新环境尽量保持一致，减少额外修改。

---

## 5.2 获取 openGauss 3.0 镜像

先拉取镜像：

```powershell
docker pull enmotech/opengauss:3.0.0
```

如果你之前使用的不是这个镜像名，而是你本地已经可用的其他 `openGauss 3.0` 镜像，也可以继续沿用，但端口、用户名和密码要与后端配置对应。

---

## 5.3 创建并运行容器

推荐直接运行以下命令：

```powershell
docker run --name opengauss ^
  -d ^
  -p 15432:5432 ^
  -e GS_PASSWORD=Gauss@2026 ^
  enmotech/opengauss:3.0.0
```

说明：

1. 容器名：`opengauss`
2. 宿主机端口：`15432`
3. 容器内数据库端口：`5432`
4. `GS_PASSWORD` 是 openGauss 默认超级管理员 `omm` 的密码

---

## 5.4 检查容器状态

```powershell
docker ps
```

看到容器处于 `Up` 状态即可。

如果要查看日志：

```powershell
docker logs opengauss
```

---

## 5.5 进入容器

```powershell
docker exec -it opengauss /bin/bash
```

进入容器后，使用 `gsql` 登录：

```bash
gsql -d postgres -U omm
```

登录成功后，提示符会变成这样 “openGauss=#”

切换到yujia_db

```
\c yujia_db;
```

---

## 6. 创建项目数据库与业务用户

项目推荐使用以下数据库和业务用户：

1. 数据库名：`yujia_db`
2. 业务用户名：`yujia`
3. 业务密码：`YuJia@123456`

在 `gsql` 中执行：

```sql
CREATE USER yujia WITH PASSWORD 'YuJia@123456';
CREATE DATABASE yujia_db;
GRANT ALL PRIVILEGES TO yujia;
\c yujia_db
GRANT USAGE ON SCHEMA public TO yujia;
GRANT CREATE ON SCHEMA public TO yujia;
ALTER SCHEMA public OWNER TO yujia;
```

然后可以进一步给数据库授权：

```sql
GRANT ALL PRIVILEGES ON DATABASE yujia_db TO yujia;
```

---

## 7. 数据库初始化说明

### 7.1 非常关键的说明

本项目数据库为alltables.sql
此外，后端启动时还有一个自动补表/补字段逻辑,启动时自行实现，不需要人工导入：

文件：
[backend/src/main/java/com/yujia/backend/common/config/SchemaInitializationConfig.java](/f:/GraduationProject-yujia/yujia-test/backend/src/main/java/com/yujia/backend/common/config/SchemaInitializationConfig.java)

但是为了让新环境最稳定、最可控，建议不要只依赖后端自动补齐，而是：

1. 先手动执行alltables.sql
2. 最后启动后端，让程序完成幂等补充。

---

## 7.2 必须执行的 SQL 文件及顺序（上面执行了alltables.sql，下面可以跳过了）

### 第一步：执行基础建表脚本

脚本文件：

[backend/src/main/resources/sql/schema-opengauss.sql](/f:/GraduationProject-yujia/yujia-test/backend/src/main/resources/sql/schema-opengauss.sql)

执行方式示例：

```bash
/usr/local/opengauss/bin/gsql -U omm -W 'Gauss@2026' -d yujia_db -f /path/to/schema-opengauss.sql
```

如果是在 Windows 中将文件拷贝到容器外执行不方便，也可以：

1. 先把脚本复制到宿主机某个好找的位置。
2. 通过数据库客户端连接执行。
3. 或把脚本内容复制进 `gsql` 执行。

该脚本会创建以下核心表和对象：

1. `sys_user`
2. `sys_role`
3. `sys_user_role`
4. `base_info`
5. `product_batch`
6. `production_record`
7. `inspection_report`
8. `trace_code`
9. `recall_record`
10. `user_feedback`
11. `product_item`
12. `scan_log`
13. `logistics_record`
14. 多个业务编号序列
15. 一系列索引

它还会自动插入三条基础角色：

1. `ADMIN`
2. `OPERATOR`
3. `USER`

---

### 第二步：执行智能升级脚本

脚本文件：

[backend/sql/manual/2026-05-06-intelligence-upgrade.sql](/f:/GraduationProject-yujia/yujia-test/backend/sql/manual/2026-05-06-intelligence-upgrade.sql)

作用：

1. 创建 `system_task` 表
2. 创建相关索引
3. 设置表 owner 和授权

执行示例：

```bash
/usr/local/opengauss/bin/gsql -U omm -W 'Gauss@2026' -d yujia_db -f /path/to/2026-05-06-intelligence-upgrade.sql
```

---

### 第三步：执行任务跟踪升级脚本

脚本文件：

[backend/sql/manual/2026-05-06-task-operation-tracking.sql](/f:/GraduationProject-yujia/yujia-test/backend/sql/manual/2026-05-06-task-operation-tracking.sql)

作用：

1. 给 `system_task` 增加 `claimed_at`
2. 给 `system_task` 增加 `completed_by_user_id`

执行示例：

```bash
/usr/local/opengauss/bin/gsql -U omm -W 'Gauss@2026' -d yujia_db -f /path/to/2026-05-06-task-operation-tracking.sql
```

---

### 第四步：执行企业隔离升级脚本

脚本文件：

[backend/sql/manual/2026-05-08-company-isolation.sql](/f:/GraduationProject-yujia/yujia-test/backend/sql/manual/2026-05-08-company-isolation.sql)

作用：

1. 创建 `company` 表
2. 自动插入“默认公司”
3. 给 `sys_user` 增加 `company_id`
4. 给 `base_info` 增加 `company_id`
5. 给 `product_batch` 增加 `company_id`
6. 给 `user_feedback` 增加 `company_id`
7. 回填历史数据
8. 创建外键和索引

执行示例：

```bash
/usr/local/opengauss/bin/gsql -U omm -W 'Gauss@2026' -d yujia_db -f /path/to/2026-05-08-company-isolation.sql
```

---

## 7.3 建议的 SQL 执行顺序总结（跳过，总表已经给到了）

请严格按下面顺序执行：

1. `schema-opengauss.sql`
2. `2026-05-06-intelligence-upgrade.sql`
3. `2026-05-06-task-operation-tracking.sql`
4. `2026-05-08-company-isolation.sql`

执行完成后，再启动后端。

---

## 7.4 后端自动补齐说明

即使你执行完上述脚本，后端启动时仍会额外检查并幂等补充：

1. `product_item`
2. `scan_log`
3. `logistics_record`
4. `user_feedback` 的 AI/任务相关字段
5. 若干索引

由于这些逻辑采用“存在则跳过”的方式，所以正常情况下不会重复报错。

---

## 8. 初始化后台可登录账号

### 8.1 非常关键

当前项目里：

1. `register` 接口只会注册普通 `USER` 角色
2. 普通 `USER` 不能登录后台管理端
3. 后台管理端只允许 `ADMIN` 或 `OPERATOR` 登录

这意味着：

仅靠前端注册页面是无法创建后台管理员账号的。

所以首次部署后，必须手工往数据库里插入一个后台用户，并给它绑定 `ADMIN` 或 `OPERATOR` 角色。（当前数据里面有一个admin，123456）

---

## 8.2 密码加密方式

后端密码加密方式见：

[backend/src/main/java/com/yujia/backend/common/auth/PasswordUtil.java](/f:/GraduationProject-yujia/yujia-test/backend/src/main/java/com/yujia/backend/common/auth/PasswordUtil.java)

当前采用：

`SHA-256`

所以数据库中保存的密码不是明文，而是 SHA-256 结果。

---

## 8.3 推荐初始化一个管理员账号

推荐初始化：

1. 用户名：`admin`
2. 密码：`123456`
3. 真实姓名：`系统管理员`
4. 角色：`ADMIN`

`123456` 的 SHA-256 值为：

```text
8d969eef6ecad3c29a3a629280e686cff8fabd63e7c6f29d2a6f4f7f7c8b8b7a
```

如果你想更稳一点，也可以在本机自行再算一次。

### 注意

如果你担心上面的 hash 值不放心，可以让部署者执行下面这个 PowerShell 命令自己计算：

```powershell
$str = "123456"
$bytes = [System.Text.Encoding]::UTF8.GetBytes($str)
$sha = [System.Security.Cryptography.SHA256]::Create()
($sha.ComputeHash($bytes) | ForEach-Object { $_.ToString("x2") }) -join ""
```

---

## 8.4 插入管理员账号 SQL

请先连接 `yujia_db`，然后执行：

```sql
INSERT INTO sys_user (username, password, real_name, phone, openid, company_id, status, created_at, updated_at)
SELECT
  'admin',
  '8d969eef6ecad3c29a3a629280e686cff8fabd63e7c6f29d2a6f4f7f7c8b8b7a',
  '系统管理员',
  '13800000000',
  NULL,
  (SELECT id FROM company WHERE company_name = '默认公司' LIMIT 1),
  1,
  CURRENT_TIMESTAMP,
  CURRENT_TIMESTAMP
WHERE NOT EXISTS (
  SELECT 1 FROM sys_user WHERE username = 'admin'
);
```

然后给该用户绑定 `ADMIN` 角色：

```sql
INSERT INTO sys_user_role (user_id, role_id)
SELECT
  su.id,
  sr.id
FROM sys_user su
JOIN sys_role sr ON sr.role_code = 'ADMIN'
WHERE su.username = 'admin'
  AND NOT EXISTS (
    SELECT 1
    FROM sys_user_role sur
    WHERE sur.user_id = su.id AND sur.role_id = sr.id
  );
```

---

## 8.5 如需创建业务员账号

如果想创建一个业务员账号，例如：

1. 用户名：`operator`
2. 密码：`123456`
3. 角色：`OPERATOR`

可执行：

```sql
INSERT INTO sys_user (username, password, real_name, phone, openid, company_id, status, created_at, updated_at)
SELECT
  'operator',
  '8d969eef6ecad3c29a3a629280e686cff8fabd63e7c6f29d2a6f4f7f7c8b8b7a',
  '业务员',
  '13900000000',
  NULL,
  (SELECT id FROM company WHERE company_name = '默认公司' LIMIT 1),
  1,
  CURRENT_TIMESTAMP,
  CURRENT_TIMESTAMP
WHERE NOT EXISTS (
  SELECT 1 FROM sys_user WHERE username = 'operator'
);
```

```sql
INSERT INTO sys_user_role (user_id, role_id)
SELECT
  su.id,
  sr.id
FROM sys_user su
JOIN sys_role sr ON sr.role_code = 'OPERATOR'
WHERE su.username = 'operator'
  AND NOT EXISTS (
    SELECT 1
    FROM sys_user_role sur
    WHERE sur.user_id = su.id AND sur.role_id = sr.id
  );
```

---

## 9. 启动后端

## 9.1 后端配置文件

后端配置文件位置：

[backend/src/main/resources/application.yml](/f:/GraduationProject-yujia/yujia-test/backend/src/main/resources/application.yml)

默认关键配置如下：

1. 端口：`8080`
2. 数据库：`jdbc:postgresql://localhost:15432/yujia_db`
3. 用户名：`yujia`
4. 密码：`YuJia@123456`

只要你前面的数据库创建就是按本文档做的，这里一般不需要改。

---

## 9.2 编译后端

进入目录：

```powershell
cd backend
```

先测试编译：

```powershell
.\mvnw.cmd -q -DskipTests compile
```

如果编译通过，说明 JDK、Maven Wrapper 和依赖都基本正常。

---

## 9.3 运行后端

```powershell
.\mvnw.cmd spring-boot:run
```

启动成功后，默认访问地址：

`http://localhost:8080`

---

## 9.4 后端健康检查

启动成功后，可以用浏览器或接口工具访问：

```text
http://localhost:8080/api/health
```

如果该接口正常返回，说明后端已经启动成功。

---

## 10. 启动后台管理端

## 10.1 安装依赖

进入目录：

```powershell
cd admin-web
```

安装依赖：

```powershell
npm install
```

---

## 10.2 启动开发环境

```powershell
npm run dev
```

启动后一般会输出一个本地地址，例如：

```text
http://localhost:5173
```

---

## 10.3 管理端接口地址配置

管理端请求地址定义在：

[admin-web/src/api/http.ts](/f:/GraduationProject-yujia/yujia-test/admin-web/src/api/http.ts)

默认值为：

`http://localhost:8080`

也支持通过环境变量：

`VITE_API_BASE_URL`

如果后端不在本机或端口不同，需要自行调整。

---

## 10.4 后台登录账号

按本文档初始化管理员后，推荐使用：

1. 用户名：`admin`
2. 密码：`123456`

如果你初始化了 `operator`，也可使用：

1. 用户名：`operator`
2. 密码：`123456`

---

## 11. 启动微信小程序端

## 11.1 导入项目

打开微信开发者工具，导入项目目录：

`uniapp`

如果导入失败，也可以尝试直接导入：

`uniapp/miniprogram`

具体以你本地微信开发者工具识别结果为准。

---

## 11.2 小程序接口地址配置

小程序接口地址配置文件位于：

[uniapp/miniprogram/config/env.ts](/f:/GraduationProject-yujia/yujia-test/uniapp/miniprogram/config/env.ts)

当前内容里有两个地址：

1. `LOCAL_API_BASE_URL = 'http://127.0.0.1:8080'`
2. `LAN_API_BASE_URL = 'http://10.195.214.101:8080'`

### 推荐配置方式

如果你在微信开发者工具本机调试：

1. 优先使用 `127.0.0.1:8080`

如果你要让真机访问本机后端：

1. 把 `LAN_API_BASE_URL` 改成你电脑在局域网里的实际 IP
2. 比如 `http://192.168.1.100:8080`

---

## 11.3 小程序登录与接口说明

小程序涉及的主要认证接口包括：

1. `/api/auth/wechat-login`
2. `/api/auth/login`
3. `/api/auth/register`
4. `/api/auth/bind`
5. `/api/auth/me`

说明：

1. 小程序普通用户可以走注册或微信登录
2. 小程序用户默认是 `USER` 角色
3. 小程序用户不能登录后台管理端

---

## 11.4 微信开发者工具设置建议

为保证本地调试顺利，建议检查：

1. 是否勾选“不校验合法域名、web-view、TLS 版本以及 HTTPS 证书”
2. 后端地址是否能从开发者工具访问
3. 若使用真机调试，本机和手机是否在同一局域网

---

## 12. AI 功能配置

## 12.1 说明

本项目中部分功能依赖 AI 服务，例如：

1. 反馈自动分类
2. AI 问答

后端配置文件中用到以下环境变量：

1. `DEEPSEEK_ENABLED`
2. `DEEPSEEK_API_KEY`
3. `DEEPSEEK_BASE_URL`
4. `DEEPSEEK_MODEL`
5. `DEEPSEEK_TEMPERATURE`
6. `DEEPSEEK_CONNECT_TIMEOUT_MS`
7. `DEEPSEEK_READ_TIMEOUT_MS`
8. `DEEPSEEK_STAFF_LIMIT_PER_MINUTE`
9. `DEEPSEEK_USER_LIMIT_PER_MINUTE`

---

## 12.2 不配置 AI 是否能运行

可以运行，但有区别：

1. 基础增删改查、追溯查询、管理端和小程序主流程可以跑
2. AI 问答功能可能不可用
3. 反馈智能分类可能不可用或效果不完整

所以如果只是本地演示基础系统，可以暂时不配 AI Key。

---

## 12.3 Windows 环境变量示例 

如果你要配置 AI Key，可在 PowerShell 中执行：（执行到这里得时候，联系我一下吧）

```powershell
setx DEEPSEEK_API_KEY "你的Key"
setx DEEPSEEK_ENABLED "true"
setx DEEPSEEK_BASE_URL "https://api.deepseek.com"
setx DEEPSEEK_MODEL "deepseek-chat"
```

配置后请重新打开终端，再启动后端。

---

## 13. 文件上传与附件说明

后端配置中包含上传目录：

1. 本地目录：`uploads`
2. 对外访问路径：`/uploads/**`

仓库中当前已经存在：

[backend/uploads](/f:/GraduationProject-yujia/yujia-test/backend/uploads)

这里面可能放有：

1. 质检报告图片
2. 演示附件

如果拉取代码后该目录为空：

1. 系统仍可运行
2. 但某些报告预览或附件展示会缺少示例文件

---

## 14. 推荐启动顺序

每次完整启动项目时，建议顺序如下：

1. 启动 Docker Desktop
2. 启动 openGauss 容器
3. 确认数据库 `yujia_db` 存在
4. 确认基础 SQL 和增量 SQL 已执行
5. 启动后端 `backend`
6. 启动后台管理端 `admin-web`
7. 打开微信开发者工具启动小程序

---

## 15. 首次运行后的验证清单

建议按下面清单确认系统是否真的跑起来了。

### 15.1 后端验证

1. 访问 `http://localhost:8080/api/health`
2. 后端启动日志中没有数据库连接报错
3. 后端启动日志中没有表不存在报错

### 15.2 数据库验证

检查以下表是否存在：

1. `sys_user`
2. `sys_role`
3. `sys_user_role`
4. `company`
5. `base_info`
6. `product_batch`
7. `production_record`
8. `inspection_report`
9. `trace_code`
10. `recall_record`
11. `user_feedback`
12. `product_item`
13. `scan_log`
14. `logistics_record`
15. `system_task`

### 15.3 后台验证

1. 打开后台登录页
2. 用 `admin / 123456` 登录
3. 成功进入后台首页
4. 左侧可看到二级折叠菜单

### 15.4 小程序验证

1. 小程序首页能正常打开
2. 账号页能调用登录 / 注册接口
3. 查询页能正常请求后端

---

## 16. 演示数据建议

如果对方只是为了“跑起来并看效果”，建议最少准备：

1. 一个后台管理员账号
2. 一条基地数据
3. 一条批次数据
4. 至少一条生产记录
5. 至少一条质检报告
6. 至少一条物流记录
7. 至少一个追溯码或单品追溯对象

这样才能完整演示：

1. 后台建档
2. 小程序查询
3. 报告预览
4. 反馈回流

如果没有业务数据，系统虽然能启动，但展示效果会比较空。

---

## 17. 常见问题排查

## 17.1 后端启动报数据库连接失败

请检查：

1. Docker 是否启动
2. openGauss 容器是否在运行
3. 宿主机端口是否是 `15432`
4. `application.yml` 里的用户名密码是否和数据库一致

---

## 17.2 后端启动报表不存在

请检查：

1. 是否先执行了 `schema-opengauss.sql`
2. 是否按顺序执行了 3 个手工增量 SQL
3. 是否连到了正确的数据库 `yujia_db`

---

## 17.3 能注册但后台不能登录

这是正常现象。

原因：

1. 注册接口默认创建的是 `USER`
2. 后台只允许 `ADMIN` 或 `OPERATOR`

解决方法：

1. 手动插入管理员账号
2. 或在数据库里给已有账号绑定 `ADMIN/OPERATOR` 角色

---

## 17.4 管理端打开了但接口全报错

请检查：

1. 后端是否启动在 `8080`
2. `admin-web` 是否在请求正确地址
3. 浏览器控制台是否有跨域或网络错误

---

## 17.5 小程序请求失败

请检查：

1. `env.ts` 中 API 地址是否正确
2. 微信开发者工具是否放开合法域名校验
3. 是否使用了电脑本机可访问的局域网 IP

---

## 17.6 AI 功能报错

请检查：

1. 是否配置了 `DEEPSEEK_API_KEY`
2. 当前网络是否能访问 AI 服务
3. `DEEPSEEK_ENABLED` 是否为 `true`

如果只是演示普通功能，可以暂时忽略 AI。

---

## 18. 最终建议

如果你准备把项目分享给别人长期维护，除了这份部署说明，最好再额外准备以下内容：

1. 一份完整数据库导出文件
2. 一份演示账号清单
3. 一份演示数据 SQL
4. 一份 `.env.example` 或配置示例
5. 一份“小程序如何切换本机 IP”的说明

这样别人不只是“能启动”，而是真正“能用、能看、能演示”。

---

## 19. 快速执行摘要

如果对方只想快速照着做，可以按下面顺序执行：

1. 安装 `Git`、`Docker Desktop`、`JDK 17`、`Node.js 18+`、`微信开发者工具`
2. 拉取项目代码
3. `docker pull enmotech/opengauss:3.0.0`
4. 启动 openGauss 容器，映射端口 `15432:5432`
5. 创建数据库 `yujia_db`
6. 创建用户 `yujia / YuJia@123456`
7. 执行 SQL：
   `schema-opengauss.sql`
   `2026-05-06-intelligence-upgrade.sql`
   `2026-05-06-task-operation-tracking.sql`
   `2026-05-08-company-isolation.sql`
8. 插入 `admin / 123456` 管理员账号并绑定 `ADMIN`
9. 启动后端：
点击idea得启动
10. 启动管理端：
   `cd admin-web`
   `npm install`
   `npm run dev`
11. 打开微信开发者工具，导入 `uniapp` 或 `uniapp/miniprogram`
12. 检查小程序 API 地址是否正确

做到这一步，项目就可以正常运行了。
